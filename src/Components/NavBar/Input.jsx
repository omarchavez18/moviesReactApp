import React from "react";

const Input = () => {
  return (
    <input
      className="form-control me-2"
      type="search"
      placeholder="Search"
      aria-label="Search"
    />
  );
};

export default Input;
